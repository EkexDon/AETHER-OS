{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window — grants access to core Tauri APIs including event listening and dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
